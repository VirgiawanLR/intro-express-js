const sum = (num1, num2) => {
  return `the result of ${num1} + ${num2} is ${num1 + num2}`;
};

const substract = (num1, num2) => {
  return `the result of ${num1} - ${num2} is ${num1 - num2}`;
};

module.exports = {
  sum,
  substract,
};
